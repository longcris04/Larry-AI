# Lấy API key tại https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-...

# --- Model cho từng tác vụ ---------------------------------------------------
# TÊN MODEL CHỈ NẰM Ở ĐÂY. Mã nguồn không ghi cứng tên model nào (xem models.js),
# nên đổi model là sửa file này rồi khởi động lại, không phải sửa code.
# Danh sách model: https://openrouter.ai/models
#
# Thứ tự dự phòng: biến của thành phần → CHAT_MODEL. Thiếu CHAT_MODEL thì /chat
# BÁO LỖI hệ thống chứ không tự chọn model thay bạn.

# Model nền — dùng cho thành phần nào không khai riêng bên dưới.
# Đây là model chạy nhiều nhất nên ưu tiên nhanh & rẻ.
CHAT_MODEL=google/gemini-2.5-flash-lite

# --- Model của từng thành viên trong hệ multi-agent --------------------------
# Mỗi agent một dòng để nâng cấp riêng được: agent nào hay sai thì đổi mình nó,
# không làm đắt phần chat thường ngày.

# 🧭 Larry Điều phối — đánh giá, phân nhóm, quyết định gọi agent nào.
# NÊN NÂNG CẤP TRƯỚC TIÊN: phân nhóm sai thì cả lượt sai theo.
SUPERVISOR_MODEL=google/gemini-2.5-flash-lite

# 🛟 Larry Đồng hành — học sinh có hành vi/ý nghĩ tự hại (rủi ro cao nhất)
AGENT_SELF_HARM_MODEL=google/gemini-2.5-flash-lite

# 🛡️ Larry Bảo vệ — học sinh là nạn nhân bạo lực học đường
AGENT_VICTIM_MODEL=google/gemini-2.5-flash-lite

# 🧩 Larry Thấu hiểu — học sinh là người gây ra bạo lực
AGENT_ACTOR_MODEL=google/gemini-2.5-flash-lite

# 🍎 Cô giáo Larry — trò chuyện thường ngày (chạy nhiều nhất)
AGENT_HOMEROOM_MODEL=google/gemini-2.5-flash-lite

# --- Model cho hai tác vụ nền, không nói chuyện với học sinh ------------------

# Tóm tắt hội thoại + chấm mức độ nguy cơ cho quản trị viên.
# Nên chọn model khá hơn model chat vì nó quyết định có gắn cờ hay không.
SUMMARY_MODEL=google/gemini-2.5-flash

# Soạn email cảnh báo gửi giáo viên chủ nhiệm.
# Bỏ trống thì tự dùng SUMMARY_MODEL.
ALERT_MODEL=google/gemini-2.5-flash

# --- Giọng nói: học sinh nói với Larry, Larry nói lại --------------------------
# Hai model này KHÔNG rơi về CHAT_MODEL như các model trên: model chat nhận chữ
# trả chữ, còn hai model dưới đây nhận/trả âm thanh. Bỏ trống dòng nào thì phần
# đó tự tắt — bỏ trống cả hai thì app chạy đúng như trước, chỉ gõ chữ.

# 🎤 Nghe: thu âm của học sinh → chữ, rồi đưa vào hệ agent như một tin nhắn gõ tay.
# Bỏ trống thì nút micro không hiện ra.
STT_MODEL=nvidia/nemotron-3.5-asr-streaming-multilingual-0.6b

# 🔊 Nói: câu trả lời của Larry → tiếng phát ra loa (chữ vẫn hiện trong khung chat).
# Bỏ trống thì Larry chỉ hiện chữ.
TTS_MODEL=google/gemini-3.1-flash-tts-preview

# Giọng đọc của Larry. Mỗi model TTS có bộ giọng riêng — xem trang model trên
# OpenRouter. Gemini TTS có Kore, Puck, Zephyr, Aoede, Leda...
#
# ĐỔI TTS_MODEL THÌ PHẢI ĐỔI DÒNG NÀY THEO. Giọng của nhà cung cấp này gửi sang
# nhà cung cấp kia là lỗi 400 ngay, và bỏ trống KHÔNG cứu được: voice.js mặc
# định "Kore" (giọng Gemini). Ví dụ fish-audio/s2.1-pro:free chỉ nhận "alloy".
# TTS_VOICE=Kore

# Ngôn ngữ gợi ý cho model nghe. Để trống thì model tự đoán.
# STT_LANGUAGE=vi

# Hạn giờ mỗi lần gọi model giọng nói, tính bằng ms (mặc định 45000). Cao hơn
# AGENT_TIMEOUT_MS vì đọc một câu dài thành tiếng lâu hơn viết ra chữ.
# VOICE_TIMEOUT_MS=45000

# (Tuỳ chọn) OPENROUTER_MODEL là tên cũ của CHAT_MODEL, vẫn còn đọc được
# OPENROUTER_MODEL=google/gemini-2.5-flash-lite

# (Tuỳ chọn) Chỉ đổi khi bạn dùng proxy tương thích OpenRouter
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# (Tuỳ chọn) Hai giá trị này được gửi kèm để OpenRouter thống kê app của bạn
# OPENROUTER_SITE_URL=http://localhost:3000
# OPENROUTER_SITE_NAME=Larry AI

# Cổng chạy backend
PORT=5000

# Khoá ký JWT — BẮT BUỘC đổi thành chuỗi ngẫu nhiên khi deploy thật
JWT_SECRET=doi-thanh-chuoi-ngau-nhien-cua-ban

# (Tuỳ chọn) Đường dẫn file lưu tài khoản, mặc định là backend/account.json
# ACCOUNTS_FILE=./account.json

# Tài khoản quản trị: cách chính là chạy lệnh  npm run create-admin
#
# Nơi deploy KHÔNG mở được terminal (Render/Railway gói free) thì khai ba biến
# dưới đây — backend tự dựng lại đúng tài khoản đó ở mỗi lần khởi động, nên ổ đĩa
# tạm bị xoá cũng không mất quyền quản trị. Đã có admin đúng email này rồi thì
# KHÔNG ghi đè.
# ADMIN_USERNAME=coLan
# ADMIN_EMAIL=co.lan@truong.edu.vn
# ADMIN_PASSWORD=dat-mat-khau-rieng-that-manh

# (Tuỳ chọn) Tóm tắt lại sau mỗi bao nhiêu tin nhắn mới
# SUMMARY_EVERY_N_MESSAGES=4

# (Tuỳ chọn) Đường dẫn file lưu phiên hội thoại
# SESSIONS_FILE=./sessions.json

# --- Email cảnh báo cho giáo viên chủ nhiệm ----------------------------------
# Tài khoản Gmail dùng để GỬI. KHÔNG dùng mật khẩu Gmail thường ở đây, phải là
# App Password 16 ký tự (bật xác minh 2 bước rồi tạo tại
# https://myaccount.google.com/apppasswords). Dán liền hoặc có dấu cách đều được.
EMAIL_USER=larryai.bluemoon@gmail.com
EMAIL_APP_PASSWORD=abcd efgh ijkl mnop

# Tên hiện ở ô người gửi trong hộp thư của giáo viên
# EMAIL_FROM_NAME=Larry AI — Tư vấn tâm lý học đường

# Địa chỉ điền sẵn ở ô "Gửi tới" (quản trị viên vẫn sửa được trước khi gửi).
#
# Email cảnh báo LUÔN đi tới địa chỉ này, và ĐI KÈM địa chỉ của giáo viên chủ
# nhiệm lớp em đó nếu ghép được (ghép theo trường + lớp — xem backend/teachers.js).
# Lớp chưa có tài khoản giáo viên được duyệt thì chỉ gửi tới đây.
ALERT_EMAIL_TO=26ai.longnh2@vinuni.edu.vn

# --- Hai biểu mẫu xin ý kiến --------------------------------------------------
# Hiện thành một khối nhỏ ở cột trái màn hình chat, mở ra tab mới khi bấm vào.
# Bỏ trống cả hai thì khối đó không hiện ra.
STUDENT_FEEDBACK_FORM=https://docs.google.com/forms/d/.../viewform
TEACHER_FEEDBACK_FORM=https://docs.google.com/forms/d/.../viewform

# --- Tài liệu hướng dẫn sử dụng ------------------------------------------------
# Hiện thành nút "Xem hướng dẫn sử dụng đầy đủ" ở mục "Dùng Larry như thế nào?"
# trên trang giới thiệu. Bỏ trống thì nút không hiện.
#
# Dùng Google Drive/Docs thì NHỚ đặt quyền chia sẻ là "Bất kỳ ai có đường liên
# kết" — để mặc định "Hạn chế" thì học sinh bấm vào chỉ gặp màn hình xin quyền.
USER_GUIDE_URL=https://drive.google.com/file/d/.../view

# (Tuỳ chọn) SMTP khác Gmail — trường có mail server riêng thì đặt 3 biến này
# EMAIL_HOST=smtp.truong.edu.vn
# EMAIL_PORT=587
# EMAIL_SECURE=false

# --- Hệ multi-agent (LangGraph) ----------------------------------------------
# Model của supervisor và từng agent khai ở mục "Model cho từng tác vụ" phía trên.

# Số lượt supervisor được hỏi khai thác trong một phiên (mặc định 5)
# MAX_PROBE_TURNS=5

# Số agent nói trong MỘT lượt: luôn là 1, không cấu hình được.
# Nhiều nhóm cùng bật thì nhóm ưu tiên cao nhất thắng
# (self_harm > victim > actor > general) — xem pickAgent trong agents/routing.js.

# Số lượt LIÊN TIẾP model không còn thấy dấu hiệu thì nhóm mới bị gỡ (mặc định 2)
# GROUP_DROP_AFTER_TURNS=2

# Timeout mỗi lần gọi model của agent, tính bằng ms (mặc định 30000)
# AGENT_TIMEOUT_MS=30000
